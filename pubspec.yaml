name: tracker_coin_package
description: A new tracker coin package developed for flutter, using HTTP dependencies and fetching the information from coingecko. The idea is to share this example package with the flutter developer community.
version: 0.0.1
homepage: https://github.com/salbisser105/tracker_package

environment:
  sdk: '>=2.18.5 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:

  